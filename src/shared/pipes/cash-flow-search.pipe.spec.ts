import { CashFlowSearchPipe } from './cash-flow-search.pipe';

describe('CashFlowSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CashFlowSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
