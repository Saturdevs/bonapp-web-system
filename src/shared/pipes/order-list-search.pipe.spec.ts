import { OrderListSearchPipe } from './order-list-search.pipe';

describe('OrderListSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
