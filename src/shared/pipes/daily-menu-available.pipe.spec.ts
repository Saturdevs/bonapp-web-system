import { DailyMenuAvailablePipe } from './daily-menu-available.pipe';

describe('DailyMenuAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new DailyMenuAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
